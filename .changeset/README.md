This directory stores Changesets files used to version the published packages
in this monorepo.

Run `pnpm changeset` to record a release note, then run
`pnpm version-packages` to update package versions and generate changelogs.
